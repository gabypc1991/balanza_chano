/*
void eeprom_read(){
  for (int i = 0; i < 5; i++){
    EEPROM.get(adr_hour_a[i], hour_a[i]);
    EEPROM.get(adr_hour_b[i], hour_b[i]);
    EEPROM.get(adr_min_a[i], min_a[i]);
    EEPROM.get(adr_min_b[i], min_b[i]);
    EEPROM.get(adr_seg_a[i], seg_a[i]);
    EEPROM.get(adr_seg_b[i], seg_b[i]);
    }
  }
*/
